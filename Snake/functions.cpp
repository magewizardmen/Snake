#include "headers/functions.h"

char** create_field(int height, int width) {

	char** field = new char*[height] {new char};

	//std::for_each(field[0], field[height - 1], [](char* ptr) { ptr = new char*; });



	return field;
}
